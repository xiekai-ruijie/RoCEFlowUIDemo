# CLAUDE.md

本文件为 AI 在本仓库中工作时提供指引。

## 核心行为准则

### 1. 编码前思考

**不要假设。不要隐藏困惑。呈现权衡。**

LLM 经常默默选择一种解释然后执行。这个原则强制明确推理：

- **明确说明假设** — 如果不确定，询问而不是猜测
- **呈现多种解释** — 当存在歧义时，不要默默选择
- **适时提出异议** — 如果存在更简单的方法，说出来
- **困惑时停下来** — 指出不清楚的地方并要求澄清

### 2. 简洁优先

**用最少的代码解决问题。不要过度推测。**

对抗过度工程的倾向：

- 不要添加要求之外的功能
- 不要为一次性代码创建抽象
- 不要添加未要求的"灵活性"或"可配置性"
- 不要为不可能发生的场景做错误处理
- 如果 200 行代码可以写成 50 行，重写它

**检验标准：** 资深工程师会觉得这过于复杂吗？如果是，简化。

### 3. 精准修改

**只碰必须碰的。只清理自己造成的混乱。**

编辑现有代码时：

- 不要"改进"相邻的代码、注释或格式
- 不要重构没坏的东西
- 匹配现有风格，即使你更倾向于不同的写法
- 如果注意到无关的死代码，提一下 —— 不要删除它

当你的改动产生孤儿代码时：

- 删除因你的改动而变得无用的导入/变量/函数
- 不要删除预先存在的死代码，除非被要求

**检验标准：** 每一行修改都应该能直接追溯到用户的请求。

### 4. 目标驱动执行

**定义成功标准。循环验证直到达成。**

将指令式任务转化为可验证的目标：

| 不要这样做... | 转化为... |
|--------------|-----------------|
| "添加验证" | "为无效输入编写测试，然后让它们通过" |
| "修复 bug" | "编写重现 bug 的测试，然后让它通过" |
| "重构 X" | "确保重构前后测试都能通过" |

对于多步骤任务，说明一个简短的计划：

```
1. [步骤] → 验证: [检查]
2. [步骤] → 验证: [检查]
3. [步骤] → 验证: [检查]
```

强有力的成功标准让 LLM 能够独立循环执行。弱标准（"让它工作"）需要不断澄清。

### 其他补充

1. 行为准则补充

   1.1. 禁止添加用户未要求的功能、禁止过度抽象、禁止无依据重构、禁止冗余错误处理、先读代码再修改、不随意创建文件、不做时间预估、失败时先诊断再重试、清理无用代码、如实汇报结果；

   1.2. 当用户要求分析问题时，只给出分析结论和修订建议，不要直接生成或修改代码。等用户明确确认后再动手修订。

2. 关于问题理解

   2.1. 核心任务：接收到我的问题后，你的首要任务是精准识别并定义我的核心意图。

   2.2. 澄清机制：若问题存在模糊、歧义或信息缺失，禁止直接作答。你必须主动向我提问以澄清细节，并可提供选项或建议，辅助我将问题表述完整、精准。

3. 关于内容输出

   3.1. 真实性原则：严格禁止臆造。对于不确定的信息或知识盲区，必须直接承认"不确定"或"不知道"，严禁编造内容。

   3.2. 溯源原则：严格禁止凭空推测。若无法找到确切的来源或依据，应明确告知"未找到来源"，不得进行缺乏依据的推断。

4. 关于任务执行

   4.1. 规则服从：必须严格遵守已设定的所有规则，此为最高优先级指令。

   4.2. 不确定处理：任务执行过程中，一旦遇到不确定的环节，无论进度如何，必须立即暂停并询问我的意见。

   4.3.路径约束：
   - 当指定方式执行失败时，严禁擅自尝试其他未授权的方式。
   - 你需要主动反馈失败原因。
   - 若你认为存在其他可行的替代方案，必须在告知失败原因后，询问我是否尝试该新方案，并给出推荐理由，等待我的明确授权。

   4.4. 任务流执行：必须严格按照任务流执行，不能改变任务顺序或跳过任务。若任务流中存在依赖关系，必须先执行依赖任务，再执行当前任务。包括但不限于系统任务流
   项目任务流、skill中的任务流、rule文件中的任务流。


## 项目概述

AIGC3.0_RoCE流路径交互原型是一个面向 AI 任务运维的 RoCE 流路径查询、拓扑还原与故障一键诊断的静态交互原型。包含四个页面：首页概览（home.html）、AI 任务分析（index.html）、流详情下钻（flow-detail.html）、故障一键诊断（fault-diagnosis.html）。所有数据均为本地 Mock，通过 `window.ROCE_MOCK_DATA` 全局挂载，无后端依赖。

## 构建与运行

```bash
# 方式一：直接浏览器打开
open index.html

# 方式二：本地 HTTP 预览（推荐）
cd "/Users/alexmac2/Documents/个人/Obsidian Vault/AI_Avatar/Knowledge/AI Job/AIGC3.0_RoCE流路径交互原型"
python3 -m http.server 8000
# 访问 http://127.0.0.1:8000

# 部署：push 到 main 分支，GitHub Actions 自动部署到 GitHub Pages
```

## 技术栈

- **语言**: JavaScript ES6+（Vanilla，无框架）
- **样式**: CSS（单文件 `main.css`，暗色诊断工作台风格）
- **标记**: HTML5，多入口静态页面
- **数据**: Mock 数据，`window.ROCE_MOCK_DATA` 全局挂载，由 `data.js` IIFE 构建派生数据
- **可视化**: SVG 内联绘制（拓扑图、趋势图、多系列下钻图），无第三方图库
- **构建/部署**: 无构建系统，GitHub Actions + GitHub Pages

## 架构

```
AIGC3.0_RoCE流路径交互原型/
├── index.html              # AI 任务 / 流路径分析入口
├── home.html               # 首页概览仪表盘
├── flow-detail.html        # 流路径详情 + 下钻面板
├── fault-diagnosis.html    # 故障一键诊断
├── AGENTS.md               # 开发指南（必读）
├── README.md               # 交付说明
└── assets/
    ├── styles/
    │   └── main.css        # 全局样式（暗色诊断风格）
    └── scripts/
        ├── data.js         # 全局 Mock 数据 + IIFE 派生数据构建
        ├── nav.js          # 全局导航交互（菜单展开/收起）
        ├── app.js          # index.html 逻辑（过滤/表格/图表/拓扑）
        ├── home.js          # home.html 逻辑（拓扑概览/告警比例/资源）
        ├── flow-detail.js   # flow-detail.html 逻辑（详情/下钻/热力图）
        └── diagnosis.js     # fault-diagnosis.html 逻辑（任务列表/树图/弹层）
```

## 核心模式

- **`refs` + `state` 对象**: 每个 JS 文件顶部定义 `refs`（DOM 缓存）和 `state`（可变状态），遵循"缓存 DOM → 定义状态 → 事件驱动更新"的响应式循环。
- **全局 Mock 数据**: 所有数据定义在 `data.js`，挂载到 `window.ROCE_MOCK_DATA`。其他脚本通过 `const data = window.ROCE_MOCK_DATA` 访问。
- **TEXT + `t()` 文本集中管理**: `app.js` 中定义 `TEXT` 常量和 `t()` 函数，便于国际化维护。
- **SVG 自绘制可视化**: 拓扑图（节点 + 贝塞尔链路）、趋势图（polyline + area）、下钻多系列图均通过 SVG DOM 内联生成。
- **深链接传参**: URL search params 传递 `flowId`、`action` 等状态，如 `flow-detail.html?flowId=flow-001`。
- **IIFE 隔离**: 所有 JS 文件均使用 `(() => { ... })()` 包裹，避免全局污染。

## 测试相关

本项目无自动化测试框架。验证方式为浏览器手动预览，确认页面交互和样式符合预期。

## 外部服务依赖

无外部服务依赖。所有数据均为本地 Mock，状态通过 URL params 传递。
